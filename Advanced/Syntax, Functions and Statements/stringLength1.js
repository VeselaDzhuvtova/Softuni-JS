function stringLength1(first, second, third) {

    let total = first.length + second.length + third.length;
    let avg = Math.floor(total / 3);

    console.log(total);
    console.log(avg)
}
stringLength1('chocolate', 'ice cream', 'cake')