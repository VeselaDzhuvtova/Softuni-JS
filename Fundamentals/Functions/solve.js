function solve(num) {
    let text = num.toString();
    let textLength = text.length;
    let result = '';
    for (let j = 0; j < textLength; j++) {

        if (text[j] == textLength) {
            console.log('result')
        }
    }
}
solve(121);